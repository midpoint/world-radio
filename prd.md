# World Radio 全球 3D 地球电台网站 PRD

> 版本：v1.0  
> 产品类型：Web 应用 / 3D 地球 / 互联网电台  
> 开发定位：免费优先、纯前端优先、无需登录  
> 核心技术：Vue 3 + Vite + TypeScript + Three.js / Globe.GL + Radio Browser API  
> 文档状态：可用于 MVP 开发

---

## 1. 产品概述

### 1.1 产品名称

暂定名称：**World Radio（世界电台）**

备选名称：

- 地球电台
- Radio Earth
- 世界之声
- Earthwaves
- 声音地球

### 1.2 产品定位

World Radio 是一个以 3D 地球为主要交互界面的全球互联网电台收听网站。

用户可以旋转地球、选择国家或城市，收听当地公开提供的互联网电台直播，也可以通过搜索快速找到指定电台、地区、语言或音乐类型。

产品不以传统电台列表为核心，而是通过“探索地球 → 发现地区 → 收听声音”的方式，让用户获得通过声音环游世界的体验。

### 1.3 一句话描述

> 旋转地球，点击世界任意角落，收听当地正在播放的电台。

### 1.4 核心价值

1. **探索感**：通过 3D 地球发现世界各地的声音。
2. **即时性**：点击电台即可尝试播放真实直播流。
3. **低门槛**：无需注册、无需付费、无需安装。
4. **全球化**：支持国家、城市、语言和音乐类型搜索。
5. **轻量化**：不自建音频服务器，优先使用公开电台流。
6. **可持续扩展**：后续可加入每日推荐、世界此刻、AI 辅助发现等功能。

---

## 2. 产品目标与非目标

### 2.1 MVP 目标

MVP 必须完成以下核心闭环：

```text
打开网站
   ↓
看到可交互的 3D 地球
   ↓
选择国家 / 城市 / 搜索电台
   ↓
查看电台列表
   ↓
点击播放
   ↓
成功收听或获得明确的失败提示
   ↓
收藏 / 切换 / 随机探索
```

### 2.2 产品目标

- 用户能够通过地球快速找到不同国家的电台。
- 用户能够通过搜索找到指定电台或音乐类型。
- 用户能够在不登录的情况下收听和收藏电台。
- 网站能够在普通桌面浏览器和主流手机浏览器上使用。
- MVP 不依赖付费 API、不需要自建音频转发服务。
- 对不可播放、失效或受地区限制的电台提供清晰反馈。

### 2.3 非目标

MVP 暂不实现：

- 用户账号系统
- 社交聊天
- 电台录音与下载
- 音频转码或重新分发
- AI 实时翻译
- AI 主播
- 用户上传电台
- 商业广告系统
- 付费会员
- 自建全球音频 CDN
- 保证全球所有电台均可播放

---

## 3. 目标用户与使用场景

### 3.1 目标用户

| 用户类型 | 主要需求 |
|---|---|
| 音乐爱好者 | 发现不同国家和风格的音乐电台 |
| 语言学习者 | 收听目标语言的真实广播 |
| 海外生活用户 | 收听家乡或熟悉地区的电台 |
| 旅行爱好者 | 通过声音探索世界 |
| 新闻听众 | 收听不同国家的新闻与谈话节目 |
| 工作 / 学习用户 | 寻找背景音乐、爵士、古典等电台 |

### 3.2 典型场景

**场景 A：探索东京**

用户打开网站，搜索“东京”，地球旋转到日本，右侧显示东京及相关地区的电台。用户选择一间电台并开始播放。

**场景 B：寻找爵士乐**

用户输入“jazz”或“爵士”，网站显示相关电台，并在地球上标记其所属国家或地区。用户可以按地区继续筛选。

**场景 C：随机环游世界**

用户点击“随机旅行”，地球飞往一个随机国家或城市，展示当地信息及可用电台。用户点击播放，感受当地声音。

**场景 D：收听家乡电台**

用户搜索熟悉的城市或电台名称，点击播放并收藏，下次进入网站可直接从收藏中收听。

---

## 4. 产品设计原则

### 4.1 地球优先

地球是产品的主要视觉和交互中心，而不是普通电台列表的背景装饰。

### 4.2 播放优先

任何视觉效果都不能妨碍用户快速找到电台并开始播放。

### 4.3 不伪造地理位置

只有具备可靠经纬度的电台才显示精确位置。缺少坐标的电台归入国家或地区列表，不虚构发射塔位置。

### 4.4 不保证所有电台可用

互联网电台可能停播、失效、限制地区或不支持浏览器播放。产品必须以明确状态处理这些情况。

### 4.5 免费优先

MVP 使用免费开源技术和公开 API，不引入必须付费的基础依赖。

---

## 5. 信息架构

```text
World Radio
│
├── 首页 / 3D 地球
│   ├── 国家探索
│   ├── 城市探索
│   ├── 电台点位
│   └── 随机旅行
│
├── 搜索
│   ├── 国家 / 地区
│   ├── 城市
│   ├── 电台名称
│   ├── 语言
│   └── 音乐类型
│
├── 电台列表
│   ├── 地区电台
│   ├── 搜索结果
│   ├── 类型筛选
│   └── 排序
│
├── 播放器
│   ├── 播放 / 暂停
│   ├── 音量
│   ├── 上一台 / 下一台
│   ├── 收藏
│   └── 睡眠定时器
│
├── 我的收藏
│   ├── 收藏电台
│   └── 最近收听
│
└── 设置
    ├── 语言
    ├── 画质
    ├── 动画效果
    ├── 数据使用
    └── 清除本地数据
```

---

## 6. 页面与 UI 设计

### 6.1 整体视觉风格

设计关键词：

**深空、地球、声音、探索、沉浸、轻量、现代。**

建议采用深色主题：

- 深蓝或近黑色背景
- 真实地球纹理
- 柔和大气辉光
- 少量星空元素
- 电台点位使用明亮但克制的光点
- 面板采用半透明深色玻璃效果
- 播放器保持清晰、高对比度

不建议加入过多粒子、复杂光污染或持续大幅度镜头运动。

### 6.2 桌面端布局

```text
┌──────────────────────────────────────────────────────────────┐
│ 🌍 World Radio       🔎 搜索国家、城市、电台、音乐       ♡ ⚙ │
├────────────────────────────────────────┬─────────────────────┤
│                                        │ 📍 日本 · 东京       │
│                                        │ 当地时间 04:32       │
│                                        │                     │
│                                        │ 东京的电台          │
│             3D 地球                    │                     │
│                                        │ ┌─────────────────┐ │
│        拖动旋转 / 滚轮缩放             │ │ ▶ 电台 A        │ │
│        点击国家 / 城市 / 光点          │ │ ▶ 电台 B        │ │
│                                        │ │ ▶ 电台 C        │ │
│                                        │ └─────────────────┘ │
│                                        │                     │
│ 🎲 随机旅行  🧭 探索地区                 │ 流行 爵士 新闻      │
├────────────────────────────────────────┴─────────────────────┤
│ ♪ 当前电台       ⏮  ⏯  ⏭       🔊 ━━━━━       ♡  ⏱           │
└──────────────────────────────────────────────────────────────┘
```

### 6.3 手机端布局

手机端采用：

- 地球全屏或接近全屏
- 顶部悬浮搜索栏
- 底部迷你播放器
- 地区和电台列表使用 Bottom Sheet
- 收藏和设置采用独立抽屉
- 地球拖动与列表滚动互不冲突

点击国家后，底部抽屉展示地区信息和电台列表，用户可向上展开、向下收起。

### 6.4 地球视觉层级

| 缩放层级 | 展示内容 |
|---|---|
| 全球视角 | 国家轮廓、少量聚合光点 |
| 国家视角 | 主要城市或地区聚合点 |
| 城市视角 | 城市电台列表及具备准确坐标的电台点 |
| 电台选中 | 高亮对应点位，显示电台信息 |

不得在全球视角一次渲染所有电台点位。

---

## 7. 核心功能需求

### 7.1 F01 — 3D 地球

**优先级：P0**

#### 功能描述

用户可以通过鼠标或触摸操作旋转、缩放地球，并点击国家、城市或电台点位进行探索。

#### 功能要求

- 支持鼠标拖动旋转。
- 支持滚轮缩放。
- 支持手机单指旋转、双指缩放。
- 支持点击国家轮廓。
- 支持国家选中高亮。
- 支持镜头平滑飞向目标地区。
- 支持恢复全球视角。
- 支持关闭自动旋转。
- 支持低性能设备降低画质。

#### 交互规则

- 首次进入网站展示全球视角。
- 用户主动操作时停止自动旋转。
- 点击国家后，镜头飞向该国家并打开地区面板。
- 点击空白区域不应误触发电台播放。
- 镜头动画应支持系统减少动态效果设置。

#### 验收标准

- 桌面和手机均可正常旋转、缩放。
- 国家点击与视觉高亮一致。
- 镜头飞行动画不会导致明显卡顿。
- 切换地区不会重建整个 3D 场景。

---

### 7.2 F02 — 国家与城市探索

**优先级：P0**

#### 功能描述

用户选择国家后，可以查看该国家的电台，并进一步按照城市或地区探索。

#### 功能要求

- 展示国家名称、国旗、当地时间。
- 展示国家下的电台列表。
- 支持城市或地区筛选。
- 有可靠坐标的电台可显示地球点位。
- 缺少坐标的电台仍可在列表中收听。
- 支持返回国家层级和全球层级。

#### 地理数据规则

Radio Browser 提供国家、州/地区及部分电台坐标，但不能保证所有电台都有准确城市信息。

因此采用：

```text
国家
  ↓
地区 / 城市聚合
  ↓
电台列表
```

对于没有准确城市坐标的数据：

- 不自动猜测精确位置。
- 不将国家中心点伪装为电台位置。
- 可以在国家列表中正常展示。
- 可以标记为“位置未提供”。

---

### 7.3 F03 — 电台搜索

**优先级：P0**

#### 搜索范围

- 电台名称
- 国家名称
- 国家代码
- 城市或地区
- 语言
- 音乐类型 / 标签

#### 搜索交互

用户输入关键词后，搜索结果按类型分组：

```text
搜索：东京

地区
  日本
  东京

电台
  Tokyo FM
  J-WAVE
  其他匹配电台
```

#### 功能要求

- 支持中文和英文输入。
- 支持常见国家、城市中文名称映射。
- 支持英文电台名称搜索。
- 支持输入防抖。
- 支持搜索加载状态。
- 支持空结果提示。
- 支持取消过期搜索请求。
- 点击地区结果时地球自动定位。
- 点击电台结果时打开电台详情或直接播放。

#### 搜索策略

前端维护一份轻量国家与主要城市多语言索引，用于识别“日本 / Japan / JP”等常见输入。

电台名称、标签和语言通过 Radio Browser 查询。

不要求 MVP 实现复杂 AI 语义搜索。

---

### 7.4 F04 — 电台列表

**优先级：P0**

#### 电台卡片内容

- 电台 Logo
- 电台名称
- 国家 / 地区
- 语言
- 音乐类型标签
- 码率（有数据时）
- 播放按钮
- 收藏按钮
- 播放状态

#### 排序方式

- 推荐
- 热门
- 名称
- 码率
- 最近收听

推荐排序可综合：

- 数据源投票数
- 数据源点击量
- 最近检查状态
- 流地址是否可用
- HTTPS 优先级
- 用户本地最近收听记录

不得将“热门”描述为实时收听人数，除非数据源明确提供该指标。

#### 分页

- 默认每页 30～50 条。
- 使用加载更多或无限滚动。
- 不一次性请求国家下的全部电台。
- 切换筛选条件时重置分页。

---

### 7.5 F05 — 电台播放器

**优先级：P0**

#### 功能描述

用户点击电台后，网站连接其公开直播流并进行播放。

#### 播放器功能

- 播放 / 暂停
- 音量调节
- 静音
- 上一台 / 下一台
- 收藏
- 显示当前电台
- 显示播放状态
- 显示错误提示
- 打开电台官网
- 睡眠定时器

#### 播放状态

```text
idle
  ↓
loading
  ↓
playing
  ↓
paused

loading / playing
  ↓
error
```

界面状态：

| 状态 | 展示 |
|---|---|
| 未播放 | 选择一间电台开始收听 |
| 连接中 | 正在连接电台… |
| 播放中 | 动态播放指示 |
| 暂停 | 显示暂停状态 |
| 失败 | 当前电台暂不可用 |
| 受限制 | 提示可能存在浏览器或地区限制 |

#### 播放策略

1. 用户点击播放。
2. 停止当前电台。
3. 清理旧的 Audio / HLS 实例。
4. 获取目标电台播放地址。
5. 判断播放格式。
6. 使用原生 Audio 或 HLS 播放方案。
7. 等待浏览器播放成功或返回错误。
8. 更新播放器状态。

#### 格式支持

- MP3：优先原生 Audio
- AAC：根据浏览器支持情况播放
- HLS：原生支持或 hls.js
- 其他格式：根据浏览器能力尝试，不承诺全部支持

#### 失败处理

- 显示明确错误。
- 提供重试按钮。
- 提供下一台按钮。
- 提供打开电台官网入口。
- 不无限自动重试。
- 不通过未经授权的代理绕过地区限制。

---

### 7.6 F06 — 随机旅行

**优先级：P0**

#### 功能描述

用户点击“随机旅行”，系统随机选择一个有可用电台的国家或城市，镜头飞向该地区，并展示当地电台。

#### 交互流程

```text
点击随机旅行
   ↓
选择随机地区
   ↓
地球镜头飞行
   ↓
显示国家 / 城市 / 当地时间
   ↓
展示推荐电台
   ↓
用户点击播放
```

#### MVP 规则

- 默认随机选择地区，不强制自动播放。
- 可在设置中开启“随机旅行后自动播放”。
- 仅从具有可用电台数据的地区中选择。
- 避免连续重复相同地区。
- 支持“再去一个地方”。

#### 后续扩展

- 随机国家
- 随机城市
- 随机音乐类型
- 按昼夜随机旅行
- 按语言随机旅行

---

### 7.7 F07 — 收藏与最近收听

**优先级：P0**

#### 功能描述

用户无需登录即可收藏电台，并查看最近收听记录。

#### 存储方式

使用 IndexedDB，轻量设置可使用 localStorage。

#### 收藏字段

```typescript
interface FavoriteStation {
  stationuuid: string;
  name: string;
  favicon?: string;
  countrycode?: string;
  country?: string;
  url?: string;
  addedAt: number;
}
```

#### 功能要求

- 收藏电台
- 取消收藏
- 查看收藏列表
- 从收藏直接播放
- 查看最近收听
- 清空最近收听
- 清空全部本地数据

#### 数据规则

收藏使用 `stationuuid` 作为唯一标识。重新播放收藏电台时，可尝试获取最新电台数据，避免长期使用失效地址。

---

### 7.8 F08 — 音乐类型与语言筛选

**优先级：P1**

#### 推荐类型

- 流行
- 爵士
- 古典
- 摇滚
- 电子
- 新闻
- 谈话
- 民谣
- 环境音乐
- 世界音乐

#### 功能要求

- 支持按标签筛选。
- 支持按语言筛选。
- 支持国家 + 类型组合筛选。
- 支持清除筛选条件。
- 标签显示可使用中文映射，但查询保留原始标签。

---

### 7.9 F09 — 睡眠定时器

**优先级：P1**

支持：

- 15 分钟
- 30 分钟
- 60 分钟
- 自定义分钟数
- 取消定时器

到达时间后停止播放，不需要后台服务。

---

### 7.10 F10 — 设置

**优先级：P1**

设置项：

- 界面语言
- 地球画质：自动 / 高 / 低
- 自动旋转开关
- 减少动画开关
- 随机旅行自动播放开关
- 移动网络省流模式
- 清除收藏与历史
- 数据来源与版权说明

---

## 8. 技术架构

### 8.1 MVP 技术栈

| 模块 | 技术 |
|---|---|
| 前端框架 | Vue 3 |
| 构建工具 | Vite |
| 开发语言 | TypeScript |
| 3D 引擎 | Three.js |
| 地球封装 | Globe.GL |
| 状态管理 | Pinia |
| 样式 | CSS / Tailwind CSS（可选） |
| 音频 | HTML5 Audio |
| HLS | hls.js |
| 本地存储 | IndexedDB |
| 电台数据 | Radio Browser API |
| 国家边界 | Natural Earth 等开放地理数据 |
| 城市数据 | 免费开放城市坐标数据 |
| 地球纹理 | NASA Blue Marble 等可合法使用资源 |
| 部署 | Cloudflare Pages 或其他静态托管 |

### 8.2 系统架构

```text
┌──────────────────────────────────────────────┐
│                 Browser                      │
│                                              │
│  Vue 3 UI                                    │
│    ├── GlobeView                             │
│    ├── SearchPanel                           │
│    ├── StationList                           │
│    ├── AudioPlayer                           │
│    ├── Favorites                             │
│    └── Settings                              │
│                                              │
│  State / Services                            │
│    ├── RadioBrowserClient                    │
│    ├── StationRepository                     │
│    ├── GeoRepository                         │
│    ├── AudioEngine                           │
│    └── LocalStorage                          │
│                                              │
└──────────────┬───────────────────┬───────────┘
               │                   │
               ▼                   ▼
      Radio Browser API      电台公开音频流
               │
               ▼
         电台目录数据
```

### 8.3 纯前端原则

MVP 不需要：

- 自建数据库服务器
- 用户认证服务器
- 音频转发服务器
- 付费地图服务
- 付费地理编码 API

但需要明确：

> 纯前端不等于离线运行。电台目录查询和直播播放仍然依赖互联网及第三方服务。

---

## 9. Radio Browser API 设计

### 9.1 数据源

主要数据源：**Radio Browser**

官方文档：

`https://docs.radio-browser.info/`

API 信息：

`https://api.radio-browser.info/`

### 9.2 接入原则

- 使用公开 API。
- 不在前端保存任何付费密钥。
- 不依赖单一固定镜像作为永久可用地址。
- 根据官方建议实现镜像发现或维护可用镜像列表。
- 使用缓存减少重复请求。
- 避免高频、无意义的大批量查询。
- 不将第三方 API 可用性视为自己的 SLA。

### 9.3 主要接口

以下为接口路径示例，实际请求主机由可用 Radio Browser 镜像决定。

#### 按国家查询

```http
GET /json/stations/search?countrycode=JP&hidebroken=true&limit=50
```

#### 按名称查询

```http
GET /json/stations/search?name=BBC&hidebroken=true&limit=50
```

#### 按标签查询

```http
GET /json/stations/search?tag=jazz&hidebroken=true&limit=50
```

#### 获取带地理信息的电台

```http
GET /json/stations/search?has_geo_info=true&limit=1000
```

实际开发中应使用分页、过滤和缓存，不应在首页一次性拉取全球所有电台。

### 9.4 电台数据模型

```typescript
interface RadioStation {
  stationuuid: string;
  name: string;

  url: string;
  url_resolved?: string;
  homepage?: string;
  favicon?: string;

  country?: string;
  countrycode?: string;
  state?: string;

  language?: string;
  tags?: string;

  codec?: string;
  bitrate?: number;
  hls?: boolean;

  votes?: number;
  clickcount?: number;

  lastcheckok?: boolean;
  lastchecktime?: string;

  geo_lat?: number | null;
  geo_long?: number | null;
}
```

### 9.5 内部标准化模型

前端不直接让所有组件依赖第三方原始字段，应在 Service 层转换。

```typescript
interface Station {
  id: string;
  name: string;
  streamUrl: string;

  homepage?: string;
  logo?: string;

  countryCode?: string;
  countryName?: string;
  region?: string;

  languages: string[];
  tags: string[];

  codec?: string;
  bitrate?: number;

  latitude?: number;
  longitude?: number;
  hasReliableGeo: boolean;

  votes?: number;
  isAvailable?: boolean;
}
```

### 9.6 API 缓存策略

建议初始策略：

| 数据 | 建议缓存 |
|---|---|
| 国家列表 | 24 小时 |
| 城市坐标静态数据 | 随版本发布 |
| 国家电台列表 | 30～60 分钟 |
| 搜索结果 | 5～15 分钟 |
| 电台详情 | 10～30 分钟 |
| 收藏电台最新信息 | 播放前按需刷新 |

缓存时间应可配置，并根据实际 API 变化和使用情况调整。

### 9.7 API 失败降级

当 Radio Browser 不可用时：

- 地球仍可正常显示。
- 本地收藏仍可查看。
- 最近收听仍可查看。
- 已缓存的电台列表可继续展示。
- 用户可尝试播放已缓存的流地址。
- 搜索显示“电台目录暂时不可用”。
- 不显示空白页或无限加载。

---

## 10. 地理数据与地球点位设计

### 10.1 数据分层

```text
地球纹理
   ↓
国家边界 GeoJSON
   ↓
国家中心 / 聚合点
   ↓
城市坐标数据
   ↓
具备可靠坐标的电台
```

### 10.2 国家数据

需要包含：

- ISO 3166 国家代码
- 中文名称
- 英文名称
- 国家中心坐标
- 国家边界
- 可选国旗信息

国家边界可使用 Natural Earth 等开放地理数据，并遵守其许可和署名要求。

### 10.3 城市数据

MVP 可内置主要城市坐标数据，例如：

```typescript
interface City {
  id: string;
  name: string;
  nameZh?: string;
  countryCode: string;
  latitude: number;
  longitude: number;
  aliases?: string[];
}
```

城市数据用于：

- 搜索城市
- 镜头定位
- 地区聚合
- 中文 / 英文名称映射

不要求第一版覆盖全球所有城市。

### 10.4 点位聚合

根据相机距离动态切换：

```text
全球视角
   ↓
国家聚合点
   ↓
国家内城市点
   ↓
城市电台点
```

要求：

- 不一次性渲染全部电台。
- 使用视野过滤。
- 使用点位聚合。
- 使用按需加载。
- 缩放层级变化时平滑切换。
- 缺少坐标的电台仅展示在列表，不生成虚假精确点。

---

## 11. 音频播放技术设计

### 11.1 AudioEngine

建议将播放逻辑封装为独立服务。

```typescript
interface AudioEngine {
  play(station: Station): Promise<void>;
  pause(): void;
  resume(): Promise<void>;
  stop(): void;

  setVolume(volume: number): void;
  mute(): void;

  getState(): PlayerState;
  destroy(): void;
}
```

### 11.2 播放流程

```text
用户点击播放
   ↓
检查用户交互授权
   ↓
停止当前音频
   ↓
清理旧播放实例
   ↓
获取播放地址
   ↓
判断格式 / 浏览器支持
   ↓
原生 Audio 或 HLS
   ↓
连接直播流
   ↓
播放成功 / 播放失败
```

### 11.3 浏览器限制

必须处理：

- 自动播放限制
- HTTPS 页面中的 HTTP 混合内容
- 不支持的音频编码
- HLS 兼容性
- 电台流失效
- 地区限制
- 网络断开
- 流地址重定向
- 音频缓冲超时

不得承诺所有 Radio Browser 电台均可在浏览器播放。

### 11.4 播放失败策略

默认策略：

1. 显示失败原因或通用错误。
2. 允许用户手动重试。
3. 提供下一台。
4. 提供电台官网入口。
5. 记录本地失败状态，避免短时间重复推荐同一失效流。

不默认使用第三方代理绕过地区限制。

---

## 12. 状态管理与数据持久化

### 12.1 Pinia Store

建议拆分：

```text
stores/
├── globe.ts
├── search.ts
├── stations.ts
├── player.ts
├── favorites.ts
└── settings.ts
```

### 12.2 Globe Store

保存：

- 当前相机位置
- 当前选中国家
- 当前选中城市
- 当前点位层级
- 是否自动旋转
- 当前画质模式

### 12.3 Player Store

保存：

- 当前电台
- 播放状态
- 音量
- 静音状态
- 播放队列
- 错误信息
- 睡眠定时器

### 12.4 本地持久化

IndexedDB 保存：

- 收藏电台
- 最近收听
- 部分电台缓存
- 用户设置

不保存用户敏感信息，不要求浏览器定位权限。

---

## 13. 性能要求

### 13.1 性能目标

以下为 MVP 验收目标，应以实际测试设备为准：

| 指标 | 目标 |
|---|---|
| 首屏 | 优先快速展示可交互地球 |
| 地球交互 | 普通桌面设备保持流畅 |
| 移动端 | 自动降低画质，避免持续卡顿 |
| 电台列表 | 分页加载，不一次渲染大量节点 |
| 搜索 | 防抖并取消过期请求 |
| 播放切换 | 清理旧音频实例，避免多个流同时播放 |
| 内存 | 长时间切换电台不持续异常增长 |

### 13.2 优化措施

- 地球纹理按设备选择分辨率。
- 使用压缩纹理或适当尺寸的贴图。
- 减少过度使用后处理效果。
- 点位按缩放层级聚合。
- 使用虚拟列表或分页。
- 音频实例复用或正确销毁。
- 避免每帧更新 Vue 响应式状态。
- 低性能设备关闭额外粒子与复杂辉光。
- 页面隐藏时降低不必要的 3D 渲染频率。

---

## 14. 免费部署方案

### 14.1 MVP 部署

```text
GitHub
   ↓
Cloudflare Pages
   ↓
静态网站
   ↓
用户浏览器
   ├── Radio Browser API
   └── 电台公开音频流
```

### 14.2 成本预估

| 项目 | MVP 费用 |
|---|---|
| Vue / Three.js / Globe.GL | 免费 |
| Radio Browser API | 免费 |
| HTML5 Audio / hls.js | 免费 |
| IndexedDB | 免费 |
| 静态网站托管 | 可使用免费额度 |
| 自建后端 | 不需要 |
| 音频服务器 | 不需要 |
| 域名 | 可选，独立域名通常需要费用 |

免费服务的额度、使用政策和可用性可能变化，正式部署前应核查当前条款。

### 14.3 后续可选后端

当访问量增加后，可以加入轻量后端或定时任务：

```text
定时同步任务
   ↓
Radio Browser
   ↓
电台目录缓存
   ↓
静态 JSON / 对象存储 / 轻量 API
   ↓
前端读取缓存
```

用途：

- 减少第三方 API 请求
- 提高首屏速度
- 维护电台健康状态
- 建立更稳定的城市索引
- 支持每日推荐

但不属于 MVP 必需范围。

---

## 15. 版权、隐私与合规

### 15.1 电台内容

Radio Browser 提供的是电台目录，不代表所有电台音频内容均可自由录制、下载或重新分发。

MVP 仅提供公开直播流播放入口。

不实现：

- 音频录制
- 音频下载
- 音频重新托管
- 未经授权的转码分发
- 绕过地区限制的代理服务

### 15.2 地图与素材

需要核查并遵守：

- 地球纹理授权
- 国家边界数据授权
- 城市数据授权
- 第三方电台 Logo 使用要求
- 第三方电台目录使用规范

产品应提供“数据来源与版权说明”页面。

### 15.3 隐私

MVP 无需登录，不主动收集精确地理位置。

用户收藏和历史默认仅保存在本地浏览器。

应提供清除本地数据功能。

---

## 16. 错误与边界场景

| 场景 | 处理方式 |
|---|---|
| API 不可用 | 展示缓存和重试入口 |
| 电台没有 Logo | 使用默认电台图标 |
| 电台没有坐标 | 保留在地区列表，不生成精确点 |
| 电台流失效 | 显示错误并提供下一台 |
| 浏览器不支持格式 | 提示不兼容 |
| HTTP 流被阻止 | 提示可能存在安全限制 |
| 电台地区限制 | 提示可能受地区限制 |
| 搜索无结果 | 展示空状态和推荐关键词 |
| 用户离线 | 展示离线状态 |
| 手机性能不足 | 自动降低地球画质 |
| 用户拒绝自动播放 | 等待用户点击播放 |
| 收藏电台已失效 | 保留收藏并提示不可用 |

---

## 17. 无障碍与易用性

- 所有按钮提供可读名称。
- 搜索和播放器支持键盘操作。
- 播放状态不能仅依靠颜色区分。
- 支持系统减少动态效果设置。
- 提供地球之外的列表探索入口。
- 手机端按钮尺寸适合触摸。
- 保证文字与背景对比度。
- 地球加载失败时仍可通过搜索和列表收听。

---

## 18. 推荐目录结构

```text
world-radio/
├── public/
│   ├── earth/
│   │   ├── earth-texture.webp
│   │   ├── earth-bump.webp
│   │   └── stars.webp
│   ├── geo/
│   │   ├── countries.geojson
│   │   ├── countries.json
│   │   └── cities.json
│   └── icons/
│
├── src/
│   ├── app/
│   │   ├── App.vue
│   │   └── router.ts
│   │
│   ├── components/
│   │   ├── globe/
│   │   │   ├── GlobeView.vue
│   │   │   ├── GlobeControls.vue
│   │   │   ├── CountryLayer.ts
│   │   │   ├── StationLayer.ts
│   │   │   └── CameraController.ts
│   │   │
│   │   ├── search/
│   │   │   ├── SearchBar.vue
│   │   │   └── SearchResults.vue
│   │   │
│   │   ├── stations/
│   │   │   ├── StationCard.vue
│   │   │   ├── StationList.vue
│   │   │   └── RegionPanel.vue
│   │   │
│   │   ├── player/
│   │   │   ├── AudioPlayer.vue
│   │   │   ├── MiniPlayer.vue
│   │   │   └── SleepTimer.vue
│   │   │
│   │   └── common/
│   │
│   ├── services/
│   │   ├── radioBrowser.ts
│   │   ├── stationRepository.ts
│   │   ├── geoRepository.ts
│   │   ├── audioEngine.ts
│   │   └── cache.ts
│   │
│   ├── stores/
│   │   ├── globe.ts
│   │   ├── search.ts
│   │   ├── stations.ts
│   │   ├── player.ts
│   │   ├── favorites.ts
│   │   └── settings.ts
│   │
│   ├── composables/
│   ├── types/
│   ├── utils/
│   ├── styles/
│   └── main.ts
│
├── tests/
├── package.json
├── vite.config.ts
└── README.md
```

---

## 19. 开发阶段与优先级

### 阶段一：基础地球与播放器（P0）

完成：

- Vue 项目初始化
- Globe.GL 地球
- 国家点击
- Radio Browser API 封装
- 国家电台列表
- HTML5 Audio 播放
- 基础错误处理

**验收结果：可以点击一个国家并成功收听一间真实电台。**

### 阶段二：完整探索体验（P0）

完成：

- 地球镜头飞行
- 国家 / 城市层级
- 搜索
- 电台点位聚合
- 固定播放器
- 收藏与最近收听
- 随机旅行

**验收结果：形成完整的全球电台探索闭环。**

### 阶段三：兼容性与移动端（P0 / P1）

完成：

- HLS 兼容
- 播放错误处理
- 手机端布局
- 低画质模式
- API 缓存
- 空状态和离线状态
- 性能优化

**验收结果：桌面与手机均可稳定使用核心功能。**

### 阶段四：体验增强（P1）

完成：

- 类型筛选
- 语言筛选
- 睡眠定时器
- 世界当地时间
- 更多随机旅行模式
- 数据来源与版权说明

---

## 20. MVP 验收清单

### 地球

- [ ] 可以旋转、缩放地球。
- [ ] 可以点击国家。
- [ ] 可以飞向指定国家。
- [ ] 可以恢复全球视角。
- [ ] 不会一次渲染全球全部电台点位。

### 电台

- [ ] 可以按国家获取电台。
- [ ] 可以搜索电台名称。
- [ ] 可以按类型筛选。
- [ ] 可以播放至少一种主流浏览器支持的直播格式。
- [ ] 可以暂停、恢复和切换电台。
- [ ] 播放失败有明确反馈。
- [ ] 不会同时播放多个电台。

### 用户体验

- [ ] 无需登录。
- [ ] 可以收藏电台。
- [ ] 可以查看最近收听。
- [ ] 可以随机旅行。
- [ ] 手机端可以正常使用。
- [ ] 地球加载失败时仍可通过列表使用。

### 数据与部署

- [ ] 使用免费公开电台目录。
- [ ] 不需要自建音频服务器。
- [ ] 不需要付费 API Key。
- [ ] API 失败时有降级处理。
- [ ] 地理位置不伪造。
- [ ] 数据来源和授权信息可查看。
- [ ] 可以部署为静态网站。

---

## 21. 后续版本规划

### V1.1 — 世界此刻

增加昼夜地球效果，并根据当地时间推荐不同地区的电台。

例如：

> 东京正在清晨，伦敦进入夜晚，纽约正值午后。选择一个地方，听听此刻的声音。

### V1.2 — 声音旅行

支持用户创建电台旅行路线：

```text
广州 → 东京 → 巴黎 → 纽约 → 里约热内卢
```

每个地点播放一段时间后自动切换到下一站。

### V1.3 — 个人声音地图

用户可以在地球上查看自己收藏过、收听过的国家和城市，形成个人声音探索地图。

### V2.0 — 智能发现

可选加入：

- 自然语言搜索
- 根据心情推荐电台
- 根据当前时间推荐地区
- 自动生成声音旅行路线
- 多语言电台简介

AI 功能应作为后续增强，不应影响免费 MVP 的核心收听能力。

---

## 22. 最终产品方向

World Radio 的核心不应该是“收录尽可能多的电台”，而应该是：

> **让用户以最自然、最有趣的方式，发现并收听世界各地真实正在发生的声音。**

第一版最重要的不是复杂功能，而是把以下体验做到顺畅：

**旋转地球 → 发现地区 → 找到电台 → 一键播放 → 收藏 → 继续探索。**

只要这个闭环足够流畅，网站就已经具备独立的产品价值。